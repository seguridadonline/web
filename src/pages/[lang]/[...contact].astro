---
import Contact from '@/components/pages/Contact.astro';
import { routes, footerRoutes } from '@/i18n/routes';

export function getStaticPaths() {
  const locales = ['en', 'fr'];
  
  const allRoutes = { ...routes, ...footerRoutes };

  return locales.map((locale) => {
    return {
      params: { 
        lang: locale, 
        contact: allRoutes.contact[locale] 
      },
      props: { lang: locale }
    };
  });
}

const { lang } = Astro.props;
---
<Contact lang={lang} />